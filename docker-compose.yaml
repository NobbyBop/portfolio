services:
  portfolio:
    build: .
    container_name: portfolio_app
    networks:
      - shared_network
    restart: unless-stopped

networks:
  shared_network:
    name: shared_network
    external: true